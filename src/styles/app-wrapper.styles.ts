// In case of other UI library we can use
//import styled from 'styled-components';
import { styled } from '@mui/material/styles';

export const AppMainWrapperDiv = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  position: 'relative',
});
